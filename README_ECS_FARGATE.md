1. I encounteder a lot of obstacles along the way. The main one was not realizing that I deploy it on multiple instances, probably fargate works like that by deploying containers to various instances. Previously I tested it on ECS EC2 where both containers were on the same instance and I assumed it will work of Fargate, but I didn't considered that I wil have to configure secrutiy groups so that erlang nodes can communicate with each other over tcp on various ports.

2. Node name - I though it is enought to set RELEASE_NODE but forgot to set `RELEASE_DISTRIBUTION=name` to use `long names`

3. Mix Release config - not 100% sure about that but I had to configure envs inside `rel/env.sh.eex` which was generated by `mix release.init`, then isnide docker I had to copy this directory and files inside were used for build the release. Setting these inside terraform in `container_environment` didnt work. 

4. service_registries - didnt work because it expected all fields but only 2 combinations were accepted in the end, so I had to fix it by wrapping map filed as optional

5. Secrutiy group ingress - if you want to allow tcp traffic on all ports, you have to specify these ports!